const fs = require('fs');

let createTripboard = fs.readFileSync('src/pages/CreateTripboard.tsx', 'utf8');

// I will use replace or just rewrite the whole thing. It might be better to just rewrite it using sed or write a new file.
